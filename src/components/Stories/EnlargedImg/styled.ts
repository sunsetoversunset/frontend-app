import styled from 'styled-components';
import { EnlargedMap, Close as _Close } from '../../Pages/About/styled';

export const EnlargedImg = styled(EnlargedMap)``;

export const Close = styled(_Close)``;