import styled from 'styled-components';
import * as StyledAbout from '../../../Pages/About/styled';

export const Callout = styled(StyledAbout.Callout)``;

export const Pre = styled.pre`
  max-width: 100%;

  code {
    max-width: 100%;
    white-space: pre-wrap;
  }
`;