import styled from 'styled-components';

export const Strip = styled.ul`
  list-style: none;
  display: flex;
  align-items: flex-start;
  max-width: 100vw;
  padding: 0;
  margin: 40px 0;

  img {
    max-width: 100%;
  }
`;